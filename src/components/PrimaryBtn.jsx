import React from 'react';

const PrimaryBtn = ({name}) => {
    return (
        <button className='bg-gradient-to-l from-Start to-End text-white px-7 py-5 border rounded-lg text-xl font-extrabold
        '>{name}</button>
    );
};

export default PrimaryBtn;